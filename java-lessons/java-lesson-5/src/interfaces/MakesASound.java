package interfaces;

public interface MakesASound {
    // All of these signatures have no implementations here, but anything using this interface must provide implementations for all of them
    public String makeSound();
}
